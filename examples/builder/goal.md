# Synthetic word-count goal

Audience: the local operator. Return a dict with an integer word_count and the
unchanged target_dashboard_path string. Whitespace delimits words. Empty text
counts as zero. A missing file raises FileNotFoundError. This fixture does not
render an image, perform financial analysis, call a provider, or write an output.
