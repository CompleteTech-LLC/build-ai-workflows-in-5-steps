# Build AI Workflows in 5 Steps — environment template
#
# Copy this file to `.env` and fill in the key for ONE provider.
# You do NOT need all three — just whichever you signed up with.
#
# The real .env file is gitignored and will never be committed.

ANTHROPIC_API_KEY=sk-ant-REPLACE_ME
# OPENAI_API_KEY=sk-REPLACE_ME
# GOOGLE_API_KEY=AIza-REPLACE_ME
